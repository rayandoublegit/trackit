"use client";

import AddCreatorPage from "../page";

// Dedicated Instagram entry point — same tool, just a memorable URL.
// The platform toggle inside the tool lets you switch; this page exists
// so /admin/add/instagram works as a direct link for IG curation.
export default function AddInstagramPage() {
  return <AddCreatorPage />;
}
